# runtime: nodejs16
# service: server

service: diggi-flex
runtime: nodejs
env: flex

network:
  name: default
  session_affinity: true

manual_scaling:
  instances: 1
resources:
  cpu: 2
  memory_gb: 2
  disk_size_gb: 10

entrypoint: node server.js